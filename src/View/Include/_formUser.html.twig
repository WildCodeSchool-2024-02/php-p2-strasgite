
<form class='form_user_edit' method="post">
	<input type="hidden" value="{{ item.id }}" name="id">
	<label for="firstname">Prénom</label>
	<input type="text" value="{{ item.firstname }}" name="firstname" id="firstname">
    <label for="lastname">Nom</label>
	<input type="text" value="{{ item.lastname }}" name="lastname" id="lastname">
    <label for="email">Votre email</label>
	<input type="text" value="{{ item.email }}" name="email" id="email">
    <label for="address">Votre adresse postale</label>
	<input type="text" value="{{ item.address }}" name="address" id="address">
    <label for="password">Votre Mot de passe</label>
	<input type="text" value="{{ item.password }}" name="password" id="password">
	<button>Submit</button>
</form>
