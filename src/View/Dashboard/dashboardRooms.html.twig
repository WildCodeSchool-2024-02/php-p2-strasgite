{% extends 'layout.html.twig' %}

{% block title %}Administration des chambres{% endblock %}

{% block banner %}
    <img src="/assets/images/Dashboardbanner.jpg" id='clip_accueil' alt="Dashboard Bannière">
{% endblock %}

{% block content %}
    <div class="dashboard">
        <div class="dashboardTitle">
            <img src="/assets/images/roue-dentee.png" alt="Logo roue crantée">
            <h2> Interface chambres </h2>
        </div>
        <div class="dashboardList">
        <ul class="listTitle">
            {% for room in rooms %}
                <li><div class="listTitleLogo">&#x1F6CC;</div> <h3>{{room.id}}. {{room.title}}</h3></li>
                <li>{{room.type}} / {{room.bed_type}}</li>
                <li>
                    {% if room.isBooked %}<strong>Reserved</strong>&nbsp;/ {{room.start_date}} - {{room.end_date}}
                    {% else %}<strong>Vacant</strong>{% endif %}                    
                </li>
            {% endfor %}
        </ul>
        <ul class="listButton">
            {% for room in rooms %}
                <li>
                    <a href="/dashboard/rooms/delete?id={{room.id}}">Delete</a>
                    <a href="/rooms/edit?id={{room.id}}">Edit room</a>
                </li>                    
                    {# <a href="/dashboard/rooms/delete?id={{room.id}}">Delete room</a> #}
            {% endfor %}
        </ul>
        </div>
        <ul class="listButton">
            <li><a href="/dashboard/rooms/add">Add room</a></li>
        </ul>        
    </div>
{% endblock %}
